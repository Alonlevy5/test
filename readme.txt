To run it locally please extract the files,
and use the command "pip install"
to install the following libraries- requests, csv, json

after that just run the file test.py

attaching example outputs.

Thanks for this great opportunity.
